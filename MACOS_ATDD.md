# macOS 适配 ATDD（2026-09-25）

目标：先写清 Given / When / Then，再驱动真实应用，发现缺陷后修复并重测。纯逻辑测试和系统实测分别记录，不能互相替代。

实机结果分别来自 `d407977596666250cbb72e88b6839ab2270741eb` 与全屏修复包 `50b570bba0cb7fcc017da2e7d53453f22629622b`。后者已完成全屏输入、隐藏应用恢复、连续回填和关闭目标保护复测；下文保留缺陷发现与修复过程。

## 验收场景

| ID | Given / When | Then | 当前结果 |
|---|---|---|---|
| A01 | 应用缺少任一必需权限，启动服务 | 拒绝录音，显示缺失权限，启动按钮禁用 | 实机通过 |
| A02 | 三项权限齐全，重启应用 | 自动进入运行中 | 实机通过 |
| A03 | 外部文本框有焦点，短按 F2 两次 | 开始一次录音，再结束转写 | 用户于 2026-09-26 确认昨天已实测通过；自动工具的注入按键不作为物理按键证据 |
| A04 | 固定语音从扬声器播放，真实麦克风录音 | 真实 ASR 返回测试语音核心内容 | 实机通过：取消系统静音后连续两轮各返回 82 个非空白字符，包含麦克风录音/语音识别/自动验收等内容；存在识别错词 |
| A05 | TextEdit 为原目标，录音中 Cmd+H 隐藏应用 | 恢复原窗口和输入元素，只在原处插入一次 | 实机通过：`d4079775` 包回填 85 字，`50b570bb` 包再次恢复隐藏的 TextEdit 并回填 82 字；焦点三项匹配均为 1 |
| A06 | 目标输入位置关闭 | 不向新窗口误粘贴，保留历史结果并提示 | 契约与实机均通过：`50b570bb` 包计时 0:08 时关闭目标标签，82 字保留历史并提示失败，未发起恢复；后方文档与关闭前逐字相等 |
| A07 | 连续进行两轮听写 | 第二轮可正常开始，历史与回填各一次 | 实机通过：`d4079775` 连续三轮回填 80、85、83 字；`50b570bb` 连续四轮回填 77、82、78、84 字，成功统计逐次增加一次 |
| A08 | 正在锁定录音时取消 | 停止录音，不插入、不生成错误的成功结果 | 实机通过：验收按钮调用正式取消命令；录音终止、历史无新增，超过计划停止时间后没有重启录音；悬浮窗按钮交互尚未单独验收 |
| A09 | 录音中撤销权限 | 停止活动录音；恢复授权后不把按住的键当新按键 | 状态机通过；实机撤销辅助功能后录音停止，62 字结果保留历史但无回填，刷新可见权限缺失。恢复同一授权后下一轮正常回填 78 字；按住物理键的恢复边沿未实测 |
| A10 | TextEdit 存在多个窗口或标签页 | 精确恢复原窗口和文本框 | 实机通过：切换原生标签后恢复原标签并写入 83 字；另将旁路标签移成独立窗口，录音中切换过去，仍恢复原文档并写入 79 字。旁路文档两次均未改变 |
| A11 | 系统剪贴板已有已知测试文本，完成回填 | 剪贴板纯文本仍与录音前一致 | 实机通过：在独立测试文档中粘贴并撤销，比较真实回填前后的文本，完全相同；没有覆盖原剪贴板。后续富文本及图片修复复验见 A28 |
| A12 | 关闭主窗口后重新打开应用 | 主窗口恢复，服务无重复实例 | 实机通过：关闭时选择最小化到托盘，自动化重新访问后主窗口恢复、服务运行，进程始终为同一个 PID 40453；未单独验证鼠标点击 Dock |
| A13 | macOS 不支持其他应用静音 | UI 明确禁用，不以全系统静音冒充 | 实机通过 |
| A14 | 按住、toggle、锁定、额外修饰键、左右修饰键、重复键 | 边沿、优先级和失活恢复符合约定 | 自动逻辑测试通过 |
| A15 | 无权限的 ATDD 驱动或并发驱动请求 | 拒绝执行；仅 debug + atdd feature 可用 | 无权限拒绝实机通过；关闭 debug assertions 后编译明确拒绝 ATDD；本轮两个真实 run 请求并发，第二个返回“已有验收在执行”，第一个准备期取消，未启动录音 |
| A16 | Windows 构建及实机同场景 | 保持既有 Windows 行为 | Windows CI 的前端测试/构建、Rust 编译和测试已通过；Windows 桌面实机场景仍未执行 |
| A17 | ASR 返回空字符串或仅空白字符 | 不触发后续处理、焦点恢复、粘贴、学习或成功历史；提示未识别到语音 | 自动测试先红后绿；18:59 实机通过：播放器暂停时返回“未识别到语音”，新增失败历史，成功条数和字符数不变 |
| A18 | AX 权限存在，但系统拒绝 PostEvent | 不发送事件，不报告粘贴成功 | 原生测试先红后绿；实测 PostEvent 已允许，旧回填现象不能归因于该权限 |
| A19 | 录音捕获原窗口后，将该窗口切入全屏 | 仍能确认原输入目标并回填；悬浮窗可见 | 输入部分实机通过：`50b570bb` 包两轮全屏分别回填 77、78 字，目标有效且焦点全部匹配；菜单仍为“退出全屏幕”。悬浮窗可见性尚无有效证据 |
| A20 | AI 助手使用真实 LLM 处理问题或选中文本 | 返回并回填真实处理结果 | TextEdit 问答、选区翻译、切换标签、静音反馈与关闭保护实机通过，见 `ee8ada2` 复验；Chrome 单标签选区翻译通过，跨标签另见 A24 |
| A21 | 打开快捷键录制后取消，或只输入普通字母 | 恢复原快捷键，非法组合不保存，其他编辑入口重新可用，录音服务可继续使用 | 实机通过：录制期间其他入口禁用；取消后保留 Ctrl(左)+Cmd(左)、Option(左)+Space、F2；输入 `a` 显示必须包含修饰键或功能键，原配置保留。随后通过真实录音驱动回填 80 个非空白字符，服务已恢复。没有据此宣称物理全局监听验收通过 |
| A22 | 系统锁屏后由用户解锁，再查看应用 | 三项授权仍有效，服务继续运行 | 实机通过：同一 `50b570bb` 包显示三项已允许、运行中；未进行锁屏期间录音测试 |
| A23 | Chrome 本地 textarea 选中固定英文，真实录制翻译要求 | 读取原选区，真实 LLM 返回翻译，正式粘贴仅替换原英文 | `4e2e1595` 包实机通过；准备阶段的延迟读回修复得到验证 |
| A24 | Chrome 录音期间切到旁路标签 | 恢复原标签后读取原选区，旁路页面不变 | `bd596fdd` 包实机通过：录音 0:06 时切换并取得旁路页面 AX/截图证据；原选区捕获匹配，正式回填只替换原英文，旁路逐字未变 |
| A25 | Chrome 原标签关闭后请求回填 | 不向其他标签写入，保留回复并提示 | `bd596fdd` 包实机通过：真实翻译返回后关闭原标签，正式粘贴提示“原窗口已关闭，已复制到剪贴板”，旁路逐字未变；此轮未覆盖录音中关闭 |
| A26 | 服务暂停、无录音或处理，执行不录音目标检查 | 创建独立测试文档并报告原生结构；不启动录音，成功统计不变 | 实机通过：仅辅助功能已允许、麦克风与输入监控未就绪时完成 Chrome 选区准备和原生诊断，统计仍为 37/2,716 |
| A27 | Chrome 助手录音期间关闭原标签 | 不读取旁路选区、不向旁路回填；处理终止且下一轮可开始 | 实机通过防误写与恢复：0:05 关闭原标签，助手无原选区，返回要求提供英文；正式回填提示原窗口已关闭，旁路逐字不变，下一轮真实翻译成功。未在选区捕获失败时提前停止 LLM 请求 |
| A28 | 剪贴板有固定富文本或图片，执行实际回填 | 原格式和内容保留 | `ba81da5` 修复后两项实机通过：助手处理/回填后仍为 18 号加粗；听写后预览仍可从剪贴板新建原图。原始失败与复验条件见下文 |
| A29 | 录音期间复制另一段固定测试文本，再完成回填 | 保留新复制的内容，不恢复过期内容 | 听写实机通过：0:07 复制固定新标记，录音结束后正确回填原文档；随后实际粘贴仍为新标记。未覆盖粘贴的约 200ms 临界区内再次复制 |
| A30 | 开启学习，在独立文档修正本轮识别结果 | 读取同一目标的修改，给出合理学习建议；无修改不产生建议 | 部分通过：修复 token 预算后，真实听写/纠词产生 3 次 Kubernetes 建议，实际通知可见；用真实建议调用正式入库命令，词库显示自动来源。5 秒内的通知按钮点击、去重和失焦分支未完成验收 |
| A31 | 关闭主窗口、重开，再退出并启动同一包 | 后台服务正常、无重复进程；重启后权限与设置保留 | 实机通过：后台重开保持 PID 73751；退出后进程消失，重开 PID 75465、三项权限有效、自动运行，后续真实听写成功。登录启动开关创建/删除正确 LaunchAgent；未注销登录 |
| A32 | 在其他已安装应用的独立输入框中听写 | 精确回填原目标；失败时保留结果，不误写 | 未执行：现有录音验收入口只允许 TextEdit/Chrome 并验证专用目标身份；自动注入热键不能启动录音。需要扩展验收入口，当前不得声称已覆盖其他应用 |

本轮场景证据：A27 Chrome `1790402903118994000`，旁路 `1790356612849844000`；ASR 1.3s、LLM 5.1s，回填尝试后统计 42→43。后续 A28 Chrome `1790403039709866000` 的选区匹配，ASR 1.3s、LLM 1.4s，正式回填精确替换原英文，统计 43→44。富文本源标记 `PTT_RICH_20260926`，首次真实粘贴确认 18 号加粗；处理后须用新的普通字体文档验格式，不能用“粘贴后撤销”的旧文档，因为其插入字体可能仍继承为加粗。A29 TextEdit `1790403194621499000`，新复制标记 `PTT_COPY_DURING_RECORDING_20260926`，统计 44→45。

## 实测方式和边界

测试输入仅为人工生成的非敏感语句：

> 这是一段自动验收测试语音。我们正在验证麦克风录音，语音识别，以及跨应用自动输入。测试编号，一二三四五。

使用 macOS `say -v Tingting` 生成 10 秒 AIFF，通过 QuickTime Player 播放（前期使用访达 Quick Look），应用真实麦克风录音、真实豆包输入法 ASR、正常听写流水线、正常 AX 焦点恢复和 Cmd+V 回填。识别文本不得由测试工具直接写入目标。

实测时录音窗口只有 18 秒，多轮工具往返可能错过播放时机。因此另生成重复六遍、约一分钟的 `ptt-atdd-speech-long.aiff`，后续先播放再启动录音，以覆盖完整录音区间。原先 Raise 和后台点击不能证明系统前台切换。诊断实际捕获到 `com.openai.codex / AXMenu` 后，废止这些场景的 TextEdit 回填结论。新版验收驱动创建独立临时文本文件，通过 NSWorkspace 打开并激活 TextEdit，验证应用、文档 URL、AXTextArea 和系统焦点全部吻合后，再调用正式录音回调捕获目标；不会直接注入目标或识别文本。

自动控制工具发送的 F2 在物理键轮询及 session event tap 下均未触发；这个事实不能证明物理键盘成功或失败。ATDD 包用显式驱动入口调用同一录音回调，5 秒后准备专用 TextEdit 文档，确认焦点后由正式回调捕获前台目标，录音 18 秒后停止。该入口不能替代 A03 物理全局按键验收。

测试驱动通过 Cargo `atdd` feature 和 `VITE_ATDD=1` 显式开启。默认构建不注册命令、不显示面板；release 开启 feature 会编译失败。无网络监听、不读取任意路径、不绕过权限、不伪造识别结果。

## 已发现问题

1. Mac 启动/重开主窗口缺失：已修复普通启动与 reopen 行为。
2. 授权后缺少启动服务入口：已加入有权限校验的按钮。
3. 物理状态轮询不能观察短按完整边沿：改用被动 session event tap、按顺序保留状态，500ms 状态恢复；新增边沿及修饰键测试先红后绿。
4. ad-hoc 重构建改变签名身份，系统旧权限可能显示开启但 API 返回拒绝：通过系统界面更新本应用 AX/输入监控记录并刷新麦克风授权可恢复。正式分发需要稳定签名。
5. ASR 空白结果仍进入成功流水线：新增 `require_transcript` 入口校验，空/纯空白返回明确错误，正常文本和原始服务端错误保持不变。空白场景先复现失败，再实现通过，已打入新包。
6. 原生快捷键发送仅检查 AX 权限，未检查 PostEvent 权限：新增 `CGPreflightPostEventAccess` 校验，拒绝时返回失败；原生测试用替身接收事件，断言拒绝时零发送、允许时一次按下/释放，先红后绿。这是失败检测缺口，不代表已证实真实回填失败的根因。
7. 实机验收目标准备不可靠：真实 ASR 成功但 TextEdit 无新增内容，后续诊断确认捕获的是 Codex 菜单，另有两次未捕获目标。这些运行不能作为 TextEdit 回填失败证据。新增专用验收文档准备及身份校验，错误应用、文档或菜单必须拒绝开始；纯原生测试通过。验收取消按钮复用正式 `cancel_locked_recording` 命令。

此报告随实测更新。待执行和受环境限制的条目不算通过。

## 可复现命令和本轮结果

- `npm run test:ts`：本机及分支 CI 最新为 86/86 通过（增加 LF/CRLF 参数化用例；下文早期记录为 85 项）。
- `OPUS_LIB_DIR=/opt/homebrew/opt/opus OPUS_STATIC=1 cargo test --manifest-path src-tauri/Cargo.toml --features atdd`：library 210 通过，4 项桌面剪贴板测试 ignored；独立入口 6+6+3 通过（重复测试，不能相加成独立用例总数）；另有 2 项文档示例 ignored。
- `sh scripts/test-macos-hotkey-native.sh`：16 项原生验收通过（4 项事件消费者、2 项发送权限/事件数量、1 项专用文档身份匹配、1 项前台请求与输入焦点、4 项标签身份与拒绝保护、4 项全屏目标与拒绝保护），未向操作系统发送按键。
- `sh scripts/build-macos-atdd.sh`：生成本地测试包与非敏感语音样本；实际等价构建已通过。脚本不会自动录音或修改系统权限。
- 签名验证通过；动态链接列表不含 Homebrew dylib。
- 默认（未启用 atdd）Rust `cargo check --features custom-protocol` 通过；默认前端 production build 通过，并确认产物中不包含“执行真实录音验收”测试面板。
- Windows 四个原生模块与基线逐字一致（UIA 仅模块引用路径不同）。这不代表 Windows 编译或实机回归通过。
- `system_profiler SPAudioDataType` 确认默认输入为 MacBook Pro 麦克风，默认输出为内置扬声器，均为 48kHz。
- 用户已完成系统解锁，并允许当前测试包访问麦克风。应用三项权限齐全，服务运行中；已观察真实录音计时和自动结束。
- 16:20、16:24 的两次录音均返回空文本。第一轮目标恢复失败；第二轮显式激活 TextEdit 后未再报目标恢复错误，但目标无新增文本。16:27 的取消场景准备未能操作到悬浮窗取消按钮，最终自动结束并返回空文本；当时 A08 尚未通过。
- 用户随后将音量调至约 79% 并要求继续；检查发现静音仍开启，按其开启声音的意图取消静音。18:05 与 18:07 两轮真实录音均完成，累计新增 164 个非空白识别字符，目标 TextEdit 均未新增文字。第一轮尝试切换播放器，第二轮尝试保持 TextEdit 前台；后续证明该工具操作不足以保证系统前台状态，因此两轮目标假设均不成立。播放器已暂停，用户设置的音量值未改变。
- 用户完成新诊断包的系统确认后，三项权限均恢复。18:55/18:57 真实 ASR 分别返回 84/77 个非空白字符，目标未捕获且 PostEvent 允许，显式提示无法回填。18:58 的取消测试通过，历史维持 11 条；18:59 的静音测试通过，仅增加一条失败记录（共 12 条），成功统计维持 7 条/325 字。两轮目标诊断均为 Codex / AXMenu。
- 新版验收驱动增加专用文档激活和精确匹配；本轮原生 7 项、TypeScript 85 项、Rust library 210 项（4 ignored）全部通过。debug + atdd 测试包构建和 ad-hoc 签名验证通过；默认前端构建通过且不包含测试面板。实机控制最初遇到锁屏；用户解锁后已重启新包、恢复三项权限并继续测试，见下列记录。
- `cargo rustc --manifest-path src-tauri/Cargo.toml --features atdd --lib -- -C debug-assertions=no` 按预期失败，错误为 `The ATDD harness must not be included in a release build`，发布门禁通过。

原生 API 依据：[Apple CGEventTapCreate](https://developer.apple.com/documentation/coregraphics/cgevent/tapcreate(tap:place:options:eventsofinterest:callback:userinfo:)) 与 [listenOnly](https://developer.apple.com/documentation/coregraphics/cgeventtapoptions/listenonly)。

### 19:44–19:47 专用文档实测

- 19:44:57：目标 `com.apple.TextEdit / AXTextArea`，PostEvent 允许。真实语音得到 83 个非空白字符，自动写入 `PushToTalk-ATDD-1790336677275738000.txt` 一次；成功统计由 7/325 增至 8/408，历史增加一条。自动工具未写入识别结果。
- 19:45:41：录音中切到既有测试标签“未命名2”；78 字 ASR 保留，但无法恢复原标签，原标签与另一个标签都未新增识别文本。恢复场景失败，误写保护有效。
- 19:46:45：录音中关闭专用目标标签；80 字 ASR 保留并显示无法自动粘贴，后面的文档保持空白。A06 通过。
- 19:47:31：录音中通过 Cmd+H 隐藏 TextEdit；78 字 ASR 保留，应用恢复可见但目标未收到回填。此为真实焦点恢复缺陷，区别于早先捕获 Codex 菜单的无效测试。
- 为 Mac 原生恢复补充 AXFrontmost 请求，仍保留完整目标验证和失败关闭策略；原生回归先失败后通过（8 项全部通过）。增加结束后的应用/窗口/元素布尔匹配与 AX 返回码诊断；不记录其他应用文本、标题或剪贴板。修复包已构建、签名验证通过；默认 Rust cargo check 和前端生产构建也通过，默认前端不包含测试面板。辅助功能已恢复，输入监控已重新登记（需重启），当前等待修复包的麦克风受保护系统确认；实机效果尚不能宣称修复完成。
- 所有测试音频已暂停，保留用户音量设置。

### 20:13 起：焦点恢复修复复测

- 修复包 `7f5909aef2245d0ebe330b8cb14020539fdd54a4` 三项权限恢复后重测。隐藏 TextEdit：85 字识别成功，但结束后目标有效=1、应用/窗口/元素焦点=0/0/0，AX 四个操作均返回成功。仅加入 AXFrontmost 仍未解决真实激活。
- 切换 TextEdit 标签：79 字识别成功，结束后目标有效=0、应用/窗口/元素焦点=1/0/0，恢复没有执行。原标签从 AXWindows 列表消失，被旧有效性判断误认已关闭。两个测试文档均未被误写。
- 第二次修复：应用恢复改用 NSWorkspace 激活已有安装，禁止新实例和版本替换，回调验证 PID 与目标仍有效；随后恢复原标签、窗口与输入元素，最后仍验证实际焦点。原生请求和 Rust 等待均设置时限。
- 捕获系统窗口标签的 AX 原生引用，以引用身份查找当前可见窗口内的标签组；不按名称或标签序号猜测。仅扫描窗口直属标签组，不搜索任意网页内容。关闭/替换标签或 AXPress 被拒绝时停止自动输入。
- 新标签回归测试先红后绿；12 项原生测试全部通过。第二次修复包构建与签名验证通过，cdhash 为 `d407977596666250cbb72e88b6839ab2270741eb`。完整 Rust library 回归 210 项通过（4 ignored），默认 cargo check 与前端生产构建通过，默认产物不含测试面板。新辅助功能记录已生效，输入监控已重新登记（等待重启）；麦克风受保护系统确认仍待用户完成，实机效果待验证。

- 已通过场景的 TextEdit 结果通过 Cmd+S 保存到其原验收文件 `PushToTalk-ATDD-1790336677275738000.txt`，内容仍包含首次真实回填的 83 字。未对文档写入伪造识别结果。

### `d4079775` 包的实际复测结果

用户完成麦克风系统确认后重启，三项权限就绪，服务运行。以下文件名均为验收驱动自行生成的独立临时文档，识别文本由正常生产流水线写入。

| 文档编号（`PushToTalk-ATDD-*.txt`） | 实际操作与结果 |
|---|---|
| `1790339179987609000` | 普通前台输入 80 字成功。尝试隐藏发生太晚，不能计作恢复场景。 |
| `1790339236802304000` | 计时 0:16 时 Cmd+H 隐藏 TextEdit；恢复原文档并输入 85 字，焦点匹配 1/1/1。 |
| `1790339299929582000` | 计时 0:12 时切到旁路标签“未命名2”；恢复原标签并输入 83 字，旁路标签只有原测试标题。 |
| `1790339374682837000` | 计时 0:16 时关闭目标标签；80 字保留历史并提示不能回填，未请求激活，后面的 83 字文档未改变。 |
| `1790339722314936000` | 预先将“未命名2”移到独立窗口，计时 0:11 时切换过去；恢复原文档并输入 79 字，独立旁路窗口未改变。 |
| `1790339790737252000` | 计时 0:13 时撤销本应用辅助功能权限；录音停止，62 字保留历史但未写入目标。手动刷新后权限缺失可见，随后恢复同一授权。 |
| `1790339910260551000` | 恢复授权后正常录音并输入 78 字。用测试文档“粘贴、读取、撤销”比较回填前后的系统剪贴板文本，完全相同，未用复制覆盖原剪贴板。 |
| `1790340021680442000` | 输入 81 字成功，但实际为普通窗口，不能算全屏验收。 |
| `1790340086514782000` | 计时 0:08 时将当前目标切入全屏，结束后菜单仍为“退出全屏幕”。原应用/窗口/输入元素诊断均匹配，但有效性为 0；85 字保留历史，目标保持原测试标题。全屏缺陷成立。 |

成功统计最终为 22 条 / 1,521 字；此统计也包含“识别完成但回填失败”，不能用统计条数代替输入成功次数。通过场景的部分结果已通过 Cmd+S 保存到原验收文件。全屏已退出，测试音频已暂停，音量未改动。

关闭主窗口时选择“最小化到系统托盘”，自动化再次访问后窗口恢复，服务仍运行；前后 CLI 检查均只有同一个 PID 40453。没有进行物理 Dock 点击测试。

剪贴板探针最初使用浏览器工具，确认其剪贴板与系统剪贴板隔离后立即清理；对应文档粘贴已撤销，不计入任何回填成功。最终 A11 使用真实 TextEdit 的系统粘贴前后对照，数据仅为本次验收语句；富文本/非文本格式仍未覆盖。

### 全屏修复

为“隐藏标签栏但原窗口及原输入元素仍选中”补充原生回归，先在 `valid(fullTarget)` 断言处失败，再修复有效性与焦点判断。补充分支只接受原窗口仍在列表、全屏属性为真、无可见标签组、原窗口及原输入元素仍相同；替换输入元素、移除原窗口、改变窗口焦点、普通模式缺失标签、可见不同标签、其他应用占据全局焦点均拒绝。

16 项原生测试通过；这只是该分支的逻辑证据，不能替代新包全屏实测。新包构建及签名验证通过，cdhash 为 `50b570bba0cb7fcc017da2e7d53453f22629622b`。完整 Rust 串行回归通过：library 210 项、4 ignored，独立入口 6+3+6 通过、文档 2 ignored。首次与打包并行执行时文档测试发生 E0463 依赖产物缺失，停止并行后完整重跑通过。

默认 Rust `cargo check --features custom-protocol` 与默认前端 production build 通过；默认前端产物不包含 ATDD 按钮或命令。新包辅助功能、输入监控已通过系统界面重新登记。20:53 系统日志明确显示旧、新麦克风签名不匹配并弹出确认；用户完成确认后刷新，三项权限均已允许，服务成功启动。

### `50b570bb` 全屏修复包实测

| 文档编号 | 结果 |
|---|---|
| `1790340903503105000` | 计时 0:11 时将目标切入全屏；77 字自动回填，结束时菜单仍为“退出全屏幕”，有效性和焦点均为 1。 |
| `1790340967423671000` | 计时 0:11 时隐藏 TextEdit；原文档恢复并回填 82 字，Workspace 已请求，焦点全部匹配。 |
| `1790341030264306000` | 计时 0:12 时切入全屏；随后 Cmd+W 未在动画中生效，因此只算第二次全屏输入通过：78 字回填，不算关闭保护。 |
| `1790341086052696000` | 工具往返错过关闭时间，实际完成普通输入 84 字；不算关闭保护。 |
| `1790341153940370000` | 计时 0:08 时点击活动目标的“关闭标签页”；82 字保留历史并提示不能粘贴。结束目标已无效，Workspace 未请求，后方 `1790341086052696000` 文档与操作前逐字相等。 |

前三轮及普通输入结果已通过 Cmd+S 保存。成功统计最终为 27 条 / 1,924 字，此数包含有识别结果但回填受阻的场景。全屏已退出，音频暂停，应用保留运行中，三项授权均有效。没有把麦克风、识别或结果检查交给用户；用户只完成自动工具无法操作的系统授权确认。

当前不能宣称完整双平台 ATDD 全绿：物理全局热键及授权恢复时的物理按住边沿、悬浮窗取消按钮与全屏显示、助手真实 LLM 链路、其他应用兼容矩阵、Windows 桌面实机行为、正式签名和分发仍未完成。测试驱动也不能替代这些系统边界的验收。

### 分支推送与双平台 CI

所有变更只推送至 `codex/macos-platform`，没有合并或推送到 `main`。

- 首轮 `7cde1ff`：macOS 检查通过；Windows 的源码结构测试因 CRLF 换行匹配失败。
- `0cd8ab5`：先用 LF/CRLF 两组输入复现失败，再调整正则接受两种换行。86 项 TypeScript 测试通过；第二轮 Windows 在 Opus 构建时遇到 CMake 4 的旧策略兼容错误。
- `9d70b71`：只为 Windows CI 设置 `CMAKE_POLICY_VERSION_MINIMUM=3.5`，保留默认豆包输入法/Opus 功能。两个平台的前端测试、前端生产构建、`cargo check`、`cargo test` 均通过；macOS 另通过 16 项原生测试。运行证据：[Platform checks #36144098644](https://github.com/yyyzl/push-2-talk/actions/runs/36144098644)。
- 完整日志核验：两端 TypeScript 均为 86 通过、0 失败；Windows Rust library 206 通过，macOS 210 通过，两端各 4 项桌面剪贴板测试 ignored；独立测试入口各为 6+3+6 通过（重复用例），各 2 项文档示例 ignored。

- `836237b`：增加两端默认功能的 debug 打包与验收附件，完整矩阵再次通过。Windows 成功生成 NSIS 安装包；macOS 成功生成 `.app`，ad-hoc 签名校验通过，包内可执行文件无 Homebrew 动态库依赖。两个附件保留 7 天，不包含 ATDD 驱动，不生成更新产物，也没有发布版本。运行及附件入口：[Platform checks #36145662582](https://github.com/yyyzl/push-2-talk/actions/runs/36145662582)。

CI 不包含 Windows 交互式桌面验收，也不包含正式 Developer ID 签名、公证、安装器安装或升级测试。Mac 当前测试包未配置 LLM 服务，且现有驱动仅有听写入口；助手验收需要专用测试入口与有效的 LLM 配置，不会把模拟响应算作真实 LLM 通过。

### 22:11 快捷键配置退出后的录音恢复

在同一 `50b570bb` 测试包中，先打开听写快捷键录制并取消，再重新录制普通字母 `a`。界面正确拒绝非法组合并保留原键位；其他入口重新可用。随后播放原非敏感验收语音、运行真实录音驱动，`1790345451295735000` 文档自动回填一次 80 个非空白字符，焦点与目标有效性全部匹配，统计由 27/1,924 增为 28/2,004。驱动会检查服务 active 状态，此场景可证明上述操作后录音服务恢复可用，不能替代物理快捷键验收。结果通过 Cmd+S 保存，音频已暂停。


## 助手 ATDD 扩展（2026-09-25 晚间）

本轮测试包 CDHash：`3a4045099c2f40a4d9b516b21497c2f14d467aa3`。用户授权使用原 Windows 配置测试；仅导入解析后的助手提供商及其绑定，Mac 其他设置保留，原文件未修改，凭据及备份均在仓库外。本段更新了前文“助手尚未配置”的历史状态。

| 场景 | Given / When | Then | 当前证据 |
| --- | --- | --- | --- |
| 助手问答 | 独立 TextEdit 文档无选区；播放固定问题，真实录音 | 正式助手流水线返回 LLM 答案；用户式插入命令回填原文档 | 实机通过；ASR 有重复问题误识别，详见下文 |
| 选中文本处理 | 独立文档选中固定英文；录制翻译要求 | 实际复制的选区匹配，返回翻译并替换原选区 | 实机通过 |
| 准备/初始化取消 | 驱动准备或初始化时取消 | 没有延迟开始录音、没有后续处理 | 自动回归及实机通过 |
| 助手录音取消 | 助手录音中取消 | 仅终止当前验收，旧请求不能终止之后的新录音 | 实机取消通过；旧请求隔离由自动回归覆盖 |
| 目标切换/关闭 | 录音期间切换标签；或回复返回后关闭原目标再粘贴 | 选区不串用，原目标失效时不向其他输入框粘贴 | 缺陷修复后实机通过，详见 `ee8ada2` 记录 |

驱动使用独立编号绑定本轮录音，并通过真实初始化任务判断麦克风是否已启动；取消/超时会终止并等待初始化退出。逻辑按键只在 `debug + atdd` 下覆盖输入采样；不向系统注入按键，不改变正式默认构建。结束后等待正式流水线事件；超时明确标为不能判定通过。选区测试仅操作驱动创建且内容匹配的 TextEdit 文档，按 UTF-16 校验设置结果，只记录正式选区复制是否匹配，不记录外部选区文本。

本轮验证：
- TypeScript：86/86 通过；ATDD 与默认前端构建通过。
- Rust `cargo test --features atdd`：library 210 通过、4 ignored；新增生命周期 4/4；ATDD 控制器入口 12/12（其中 6 项是复用的原状态机测试）；其余独立入口 6+3+6，包含重复执行；文档示例 2 ignored。
- 原生测试：20/20，其中新增 4 项覆盖 Unicode 选区、空选区、错误焦点/内容/范围拒绝、AX 写入失败或未生效拒绝。
- `cargo check --features atdd` 与默认 `cargo check` 通过。
- 本地 `.app` 构建、签名严格校验通过，主程序未依赖 Homebrew dylib。
- 实机仍等待系统身份验证完成后恢复同一应用的三项权限；上述自动测试不代替真实助手结果、回填和交互验收。

### 助手边界的失败证据（23:14–23:18，后由 `ee8ada2` 修复）

- 静音：Given 播放器暂停、助手选区模式；When 录音正常结束；Then 应返回明确无语音反馈并恢复空闲。`1790349258622201000` 文档对应实测失败：后端空文本分支仅返回，缺少终止事件；驱动 90 秒后超时，前端仍忙碌，成功统计未增加。
- 切换标签：Given 原文档选中固定英文；When 录音期间切至已翻译的旁路文档；Then 应恢复原标签后读取原选区，旁路文档不变。`1790349437764125000` 对应实测失败：实际选区捕获“不符合场景”，LLM 要求提供英文；未回填。原始目标仍有效，当前焦点不匹配。


### `66cdd19` 本机和 CI 结果

- [Windows/macOS CI](https://github.com/yyyzl/push-2-talk/actions/runs/36151410599) 全部通过，包含默认构建、TS 86 项、Rust library（Windows 206 / Mac 210，均 4 ignored）、新异步生命周期 4 项、ATDD 控制器 12 项入口、原有独立测试、Mac 原生 20 项、Windows NSIS/Mac `.app` 调试打包。
- CI 原型包：[Windows](https://github.com/yyyzl/push-2-talk/actions/runs/36151410599/artifacts/10871408381)、[Mac](https://github.com/yyyzl/push-2-talk/actions/runs/36151410599/artifacts/10872220202)，保存 7 天；不含 ATDD feature 或本地凭据。
- 原助手接口 TLS 域名校验失败。未绕过校验；改用同一份授权配置中的 DeepSeek，直接连接检查返回 HTTP 200 且正确回答 `2`。Mac 助手页确认实际模型 `deepseek-v4-flash`。这只是服务前置检查，不替代下面的实机结果。
- 问答：`1790349008344898000`；真实 ASR 1.4 秒 / LLM 8.8 秒。循环语音中的重复 `1+1` 被 ASR 识别为连续几道算术题；LLM 按识别结果返回 `2 3 4 5 6`。正式 `paste_latest_reply` 恢复原文档并回填，文档已保存；成功统计 29→30。这证明链路及回填，不代表 ASR 逐字准确。
- 选区：`1790349157734078000`；正式复制结果与固定英文完全匹配，ASR 1.3 秒 / LLM 1.6 秒，结果 `会议明天三点开始。`，实际粘贴仅替换原选区，文档已保存；成功统计 30→31。
- 取消：实机分别返回“已取消准备，没有启动录音”“已取消录音初始化，没有启动后续处理”和“已取消本轮录音”。最后一项文档 `1790349556590227000`，取消时可见录音计时，随后恢复运行中，成功统计仍为 32，没有在计划停止点重新启动。上述取消使用 debug 驱动的安全清理入口，不等同于悬浮窗取消按钮或物理按键验收。
- 切换标签失败用例关闭结果面板后计入一条真实问答历史（31→32），但未插入任何文档；不能计作选区处理成功。

### `ee8ada2` 助手修复复验

修复包 CDHash：`4b0a3798a07a87410a9e376540fc6ffb72cf3645`。三项权限恢复后由自动化完成录音、结果与目标内容检查。

- 静音 `1790350074736460000`：播放器暂停，录音正常结束后出现“未识别到语音，请检查麦克风输入后重试”；驱动收到失败终止事件，应用恢复运行中，成功统计仍为 32。没有再次等待 90 秒超时。负向场景通过。
- 切换标签 `1790350148449482000`：录音期间切至旁路文档 `1790349157734078000`；结束时正式选区复制匹配，返回“会议明天三点开始。”（ASR 1.3 秒 / LLM 1.4 秒）。正式粘贴命令恢复原标签，仅替换原英文并保存；旁路文档逐字未变。统计 32→33。
- 关闭目标 `1790350256107230000`：选区翻译完成（ASR 1.3 秒 / LLM 1.9 秒）后、粘贴前关闭原标签。粘贴命令提示“原窗口已关闭，已复制到剪贴板”，后方文档 `1790350148449482000` 未变。此项覆盖回复返回后的关闭保护，不能宣称助手录音中关闭也已覆盖。统计 33→34。
- 修复前 `input_target_contract` 6 项基线通过；修复后 TypeScript 86 项、Rust library 210 项（4 ignored）、独立入口 4+12+6+3+6（含重复测试）、原生 20 项、默认 Cargo 编译、ATDD 打包和严格签名校验均通过。
- [Windows/macOS CI #36154086205](https://github.com/yyyzl/push-2-talk/actions/runs/36154086205) 全绿，含两端默认构建、测试及原型打包。[Windows 原型](https://github.com/yyyzl/push-2-talk/actions/runs/36154086205/artifacts/10872797131) / [Mac 原型](https://github.com/yyyzl/push-2-talk/actions/runs/36154086205/artifacts/10872304316) 保存 7 天，不含本地配置或 ATDD 驱动。

这两项修复分别在复制选区前恢复原目标、在助手空识别分支发送终止错误事件。恢复后的焦点验证和既有剪贴板时序保护保留。所有提交只在 `codex/macos-platform`；原 `main` 未修改。

## Chrome ATDD 驱动扩展

测试面板新增应用选择。Chrome 场景仅创建独立本地 HTML textarea，正文仍为相同的固定非敏感语句；使用正式目标捕获、录音、ASR、LLM 与粘贴流程。原生准备阶段验证应用 bundle、文件 URL 的本机来源、精确路径、AXTextArea、内容与选区，拒绝其他应用、文件、地址栏以及同路径网络 URL。

原生测试先在 Chrome 本地文档断言失败，扩展后 21 项通过。TypeScript 86 项、Rust library 210 项（4 ignored）、相关独立入口、默认编译、默认前端不含驱动的检查、ATDD 打包及严格签名校验通过。测试包 CDHash：`5e6ce3a776acb7a3d68c269a30c5c0efa1d24b31`。Chrome 实机场景尚待麦克风系统确认完成，不计为通过。

### Chrome 首轮及选区准备复验（2026-09-26）

- `9e5775d` 的 [Windows/macOS CI #36156308124](https://github.com/yyyzl/push-2-talk/actions/runs/36156308124) 已全部通过。
- 首轮本机 Chrome 文件 `1790353913790156000`：选择器实际仍为听写模式，因此不能记录为助手选区测试。文件、应用、输入框及系统焦点校验成功，但选区准备返回失败，驱动在启动录音前终止，统计仍为 34/2,456。CUA 随后能选中文本标题，AX 读取的固定正文及换行正确。
- 为定位选区读回时序，增加“AX 写入成功、读回延迟更新”的原生用例，先红后绿。准备逻辑只写一次，最多短暂等待读回匹配，同时持续检查原焦点和内容；失败诊断只包含阶段与 AX 状态码，不输出外部文本。22 项原生回归通过。
- 新测试包 CDHash `4e2e1595c4247daca8576b2d9ad8e64529c5de9e` 构建及严格签名校验通过。此时尚不能证明 Chrome 故障已解决，需新包实机读回验证。
- 桌面自动化曾出现按键无效，重建工具会话后恢复，随后辅助功能和输入监控授权已重新登记。00:38 系统日志确认新包请求麦克风；等待用户完成受保护系统确认，Chrome 实机仍未计为通过。

### `2c36b7f` Chrome 真实选区及跨标签证据

[Windows/macOS CI #36162135591](https://github.com/yyyzl/push-2-talk/actions/runs/36162135591) 全绿。恢复授权后执行以下实机验收，包 CDHash 为 `4e2e1595c4247daca8576b2d9ad8e64529c5de9e`：

| 文档编号 | 实机结果 |
|---|---|
| `1790356612849844000` | Chrome 选区模式准备成功，录音时 AX 显示完整固定英文被选中。实际流水线捕获选区匹配，ASR 1.4 秒 / LLM 4.5 秒，返回“会议明天三点开始。”。正式粘贴命令回到原网页，仅替换英文，保留标题及换行；统计 34/2,456→35/2,541。 |
| `1790356685262341000` | 录音 0:10 时切到上一轮已翻译的旁路标签。结束目标仍有效但标签诊断为 `0/0`，原选区未捕获，LLM 要求提供英文；ASR 1.7 秒 / LLM 7.0 秒。本轮未回填，结束会话后统计增至 36/2,630，不能把统计增加算作成功。旁路页面逐字未变。 |

因此短暂等待读回已解决 Chrome 准备阶段问题，但还发现原生窗口标签识别只检查直接子节点，无法识别 Chrome 嵌套的垂直标签栏。新增回归先在捕获 Chrome 原标签的断言处失败。修复通过原生容器查找标签组和 `AXTabButton`，保留 AX 元素身份，不使用标题或顺序代替身份；跳过网页、按钮等控件的内部内容，并限制节点数、深度、标签数及时间。新增覆盖跨标签保留原身份、同标题替换拒绝、网页内伪标签排除和循环树终止。27 项原生测试通过；新包实机仍待复验。

角色映射另核对了 [Chromium 官方 AX 回归示例](https://chromium.googlesource.com/chromium/src/+/6136f8637ff1fe2c12291d35b51ee7cf97e74f26%5E%21/)；该示例也说明网页本身会暴露相同的标签角色，因此不能无边界搜索整棵辅助功能树。

跨标签修复包 CDHash：`ce073bb241b874cca26ebf3e54c7e5dd52a89de0`。本地 TypeScript 86 项、Rust library 210 项（4 ignored）、独立入口 4+12+6+3+6（含复用用例）、原生 27 项、默认 Cargo 编译、ATDD 构建和严格签名检查均通过。恢复辅助功能授权时，系统要求 Touch ID／密码重新解锁“隐私与安全性”；尚未完成该系统验证，因此新包跨标签、关闭及 TextEdit 回归尚未执行，不能记为实机通过。

### `9704b85` 双平台 CI 复核

[Platform checks #36167115218](https://github.com/yyyzl/push-2-talk/actions/runs/36167115218) 全部通过。已核对两端测试和构建步骤，并确认 macOS 原生测试执行了新增的浏览器标签身份、切换和关闭用例；Windows NSIS 与 macOS `.app` 原型均成功生成。

原型附件：[Windows](https://github.com/yyyzl/push-2-talk/actions/runs/36167115218/artifacts/10878615081) / [macOS](https://github.com/yyyzl/push-2-talk/actions/runs/36167115218/artifacts/10878072800)，保存 7 天，不包含本地配置或 ATDD 驱动。此 CI 结论不代替 Chrome 跨标签的本机验收。再次检查桌面时，系统仍停留在 Touch ID／密码验证框，新包实机复验尚未开始。

### `9704b85` 实机复验未通过与无录音诊断入口

恢复三项权限后，包 `ce073bb241b874cca26ebf3e54c7e5dd52a89de0` 在 Chrome 文档 `1790362845145144000` 完成真实选区翻译：实际选区匹配，ASR 1.3 秒 / LLM 1.7 秒，结果为“会议明天三点开始。”。本轮切换发生在结果返回后，不能计为录音中切换用例。

粘贴前切到已翻译的旁路标签 `1790356612849844000`，正式命令返回“无法恢复原输入位置，已停止自动粘贴，请手动复制结果；结果已复制到剪贴板”。旁路页面逐字未变；统计 36/2,630→37/2,716。开始及结束目标均为 `标签=0/0`，因此嵌套容器修复仍未识别此机 Chrome 的标签身份。已核对包签名和二进制符号，确认实际运行了新遍历代码，不能把单元测试及 CI 通过视为此问题已解决。

为获取实际原生结构，ATDD 面板增加“准备并检查目标（不录音）”：要求服务暂停且没有现有录音、处理或助手会话，复用独立文档、固定正文及精确选区准备，随后立即返回目标诊断，不进入录音、ASR 或 LLM。正常录音入口仍要求服务运行及全部权限。原生诊断仅输出角色、子角色、子节点数量及层级；跳过网页和文本控件内部内容，限制时间、深度及节点数，不输出标题、正文或 URL。新增诊断用例先在缺失层级信息的断言处失败，随后通过；同时验证网页子树排除及循环树终止。

诊断包 CDHash `56684cb379642c1870bcb509b87a6d6ed8edd49f` 已构建并通过严格签名校验。刷新现有辅助功能授权时系统再次要求 Touch ID／密码，尚待完成；无录音实机检查尚未执行，Chrome 根因仍待原生结构证据确认。

本轮本地验证：TypeScript 86 项，Rust library 210 项（4 ignored）及独立入口 4+12+6+3+6（含复用用例）、原生 29 项均通过；默认 `cargo check`、默认与 ATDD 前端构建、ATDD `.app` 打包及严格签名检查通过。

`e655fb3` 的 [Windows/macOS CI #36178355390](https://github.com/yyyzl/push-2-talk/actions/runs/36178355390) 全部通过，含两端默认编译、测试及原型打包，Mac 原生验收与签名检查也通过。原型附件：[Windows](https://github.com/yyyzl/push-2-talk/actions/runs/36178355390/artifacts/10883092880) / [macOS](https://github.com/yyyzl/push-2-talk/actions/runs/36178355390/artifacts/10882744914)，保存 7 天，不含本地配置或 ATDD 驱动。系统仍停留在 Touch ID／密码验证框，因此无录音检查与 Chrome 跨标签复验仍未完成。

### Chrome 垂直标签深度实测与修复（2026-09-26 13:27 起）

解锁并恢复辅助功能后，在无麦克风和输入监控授权、服务停止的状态执行不录音检查。文档 `1790400451198596000` 的正文、选区及焦点全部匹配，统计仍为 37/2,716。实际原生结构为 `AXWindow → AXGroup ×4 → AXTabGroup → AXGroup → AXScrollArea → AXGroup ×2 → AXRadioButton/AXTabButton`。标签按钮位于窗口下第 10 层，而遍历从窗口首子节点以 0 开始计数并在深度大于 8 时停止，因此恰好漏掉该层。

新增与实测层级一致的原生回归，在旧代码的标签身份断言处失败。深度上限改为 16，同时保留 256 节点、128 标签、0.25 秒遍历限制及网页/控件子树排除。测试覆盖该深度下的活动与非活动标签身份、关闭后的同属性替代拒绝及异常超深树终止；原生测试 32 项通过。Rust library 210 项（4 ignored）及独立入口 4+12+6+3+6（含复用）、默认 Cargo 编译、ATDD 前端及 `.app` 构建、严格签名校验通过。

修复包 CDHash `bd596fdd9887308a237eff06fae4b46e4e73404c` 已完成不录音实机复验：Chrome `标签=1/0`、目标有效、应用/窗口/输入框焦点均为 1、选区准备匹配，统计仍为 37/2,716。这证明标签捕获已修复，不等同于跨标签恢复或回填已通过。辅助功能及输入监控已重新登记；13:37 系统日志确认新签名请求麦克风，完整录音复验待受保护确认完成。

### `c3d97db` Chrome 与 TextEdit 完整链路复验

用户完成当前包的麦克风确认后，三项权限均就绪，服务恢复运行。下列测试使用真实麦克风、ASR、LLM 和正式回填命令，未由自动工具写入识别或翻译结果。

| 文档编号 | 实测与结论 |
|---|---|
| Chrome `1790401453950772000` | ASR 1.3 秒 / LLM 1.1 秒；原选区匹配。翻译返回后切到旁路 `1790356612849844000`，正式回填恢复原标签，正文精确为 `PushToTalk ATDD\n\n会议明天三点开始。\n`；旁路逐字未变。统计 37→38。本轮错过录音中切换时机，仅计为回复返回后切换的通过证据。 |
| Chrome `1790401565622073000` | ASR 1.4 秒 / LLM 2.7 秒，翻译和正式回填成功，统计 38→39。曾在 0:12 发起切换，但未取得切换后、录音结束前的有效页面证据，不能独立计为录音中切换通过。 |
| Chrome `1790401668989156000` | 录音 0:06 时切至旁路，AX 与截图均确认旁路标签已选中、原标签未选中。结束后原英文选区匹配，ASR 1.3 秒 / LLM 1.5 秒，返回“会议明天三点开始。”。正式回填仅替换原英文，原目标文档标题及换行保留；旁路逐字未变。统计 39→40，A24 通过。 |
| Chrome `1790401757259938000` | ASR 1.3 秒 / LLM 2.8 秒。回复返回后关闭原标签，并确认该标签从 AX 树消失。正式回填提示“原窗口已关闭，已复制到剪贴板”，后方旁路逐字未变。统计 40→41，A25 通过。 |
| TextEdit `1790401871768240000` | 录音 0:09 时通过 Cmd+H 隐藏 TextEdit；真实原英文选区捕获匹配，ASR 1.3 秒 / LLM 1.3 秒。正式回填返回原文档，只替换原选区并保存；最终正文与上表固定预期完全一致。统计 41→42，浏览器修复后的 TextEdit 回归通过。 |

最后状态为运行中、成功统计 42/3,161，测试音频已暂停。额外尝试外部 TextEdit 的自动 F2，仍未启动录音，因此物理全局快捷键继续保留为未通过。录音期间查看应用 Window 菜单仅能操作主窗口，没有可选的 Recording Overlay；未取得悬浮窗按钮及全屏可见性的有效证据，不能宣称完成。该额外静音录音自然结束后返回“未识别到语音”，成功统计保持 42/3,161，没有误回填；它不算悬浮窗取消通过。

`c3d97db` 的 [Windows/macOS CI #36221417976](https://github.com/yyyzl/push-2-talk/actions/runs/36221417976) 全部通过，包含两端默认编译、测试、Mac 32 项原生验收及原型打包。附件：[Windows](https://github.com/yyyzl/push-2-talk/actions/runs/36221417976/artifacts/10899254186) / [macOS](https://github.com/yyyzl/push-2-talk/actions/runs/36221417976/artifacts/10898677991)，保存 7 天，不含本地凭据或 ATDD 驱动。完整平台矩阵仍有物理快捷键、悬浮窗/全屏视觉、Windows 桌面行为、其他应用及分发等未完成项，不等同于正式发布验收完成。

### 本轮独立功能验收（2026-09-26，沿用 `bd596fdd` 包）

用户补充 F2 已于昨天实测通过，A03 采用该用户证据。本轮未重新构建应用，未触发重新授权；源代码未修改。

**剪贴板缺陷（P0）**

- 富文本：TextEdit 的专用 RTF 源含 `PTT_RICH_20260926`（18 号加粗）。复制后在空白 RTF 文档实际粘贴，确认格式存在。Chrome `1790403039709866000` 的真实选区翻译和正式回填成功后，把剪贴板粘贴到另一份带 `AFTER: ` 前缀的 12 号普通字体 RTF 文档，文字相同，但加粗和原字号丢失。结果存于本机测试文件 `/tmp/ptt-atdd-rich-after.rtf`。
- 图片：通过预览打开仓库 `src-tauri/icons/128x128.png`，全选复制，并成功“从剪贴板新建”得到图片，证明初始剪贴板确有图像。TextEdit `1790403544122159000` 真实听写后（统计 46→47），预览的“从剪贴板新建”变为禁用；粘贴到专用 RTF 文档得到与本轮听写一致的文字，图像已丢失。
- 代码对应：`ClipboardGuard` 只用 `get_text`/`set_text` 保存和恢复；`TextInserter::insert_text` 同样只保存纯文本，初始剪贴板没有文字时不恢复。两个真实失败覆盖助手与普通听写两条路径，不能只修其中一条。
- 录音期间新复制文字（A29）通过；恢复前约 200ms 内再次复制的并发竞争仍未验证，不能外推为完整剪贴板并发安全。

**自动学习缺陷与验证边界（P1）**

- 临时开启自动学习，使用固定测试音频“这次测试使用库伯内特斯管理容器，服务运行正常”，在真实回填后把一个音译词改为 `Kubernetes`。`1790403194621499000` 第一轮的模型绑定没有切换成功，不计为有效模型验收。
- 后续确认本地配置绑定 `accdc6a3-725` / `deepseek-v4-flash`、学习开启、词库尚无 Kubernetes；`1790403345757951000` 和 `1790403867014005000` 完成真实回填和修正。最后一轮通过测试包 Web Inspector 注册只读 `vocabulary_learning_suggestion` 事件观察器，超过观察期后事件列表仍为空，未形成端到端通过证据。
- 从 `learning/llm_judge.rs` 提取实际系统提示，以固定非敏感修正内容、相同模型和参数直接请求已授权 API。`max_tokens=256` 的两次响应均正文为空；第二次明确 `finish_reason=length`，256 个 completion token 全为 reasoning token，耗时 1.60s。对照只改为 `max_tokens=1024`，3.02s 返回 `finish_reason=stop` 和有效 JSON：`should_learn=true`、`word=Kubernetes`、`category=term`。这直接复现现有学习请求与该模型不兼容；并不证明应用中的每个原生观察步骤已经走通。
- 完整建议显示/加入、同目标去重、旧任务清理、失焦和不支持控件的实机矩阵仍待完成。模型预算对照属于 API 诊断，不替代这些场景。

**后台生命周期、并发与环境**

- 关闭主窗口选择后台运行，再重新访问主窗口，PID 始终 73751 且运行中。Cmd+Q 后进程退出；重启同一包产生唯一 PID 75465，自动运行、三项权限保留、历史统计 47/3,564 保留。重启后真实听写成功，统计 48/3,642。
- 登录启动开关开启时生成 `~/Library/LaunchAgents/PushToTalk.plist`，`RunAtLoad=true`，可执行文件为当前包，参数为 `--minimized`。关闭后条目删除。未实际注销/重新登录。
- 从本地调试面板调用两个正式 `run` 命令，第二个拒绝“已有验收在执行”；取消第一个后返回“已取消准备，没有启动录音”，统计不变。没有绕过权限、改变目标检查或伪造录音结果。
- 额外静音录音时，应用窗口 API 返回 `overlay` 可见，但自动化仍只能取得主窗口，无法取得悬浮窗按钮/全屏视觉证据。调试尝试聚焦该窗口也未取得有效控件证据，不计通过。该轮正常以“未识别到语音”结束，统计仍为 48/3,642。控制台重复提交的请求被并发保护拒绝，没有额外活动录音。
- 系统只枚举到内置麦克风与内置扬声器，无法执行两个输入设备之间的切换。其他应用的完整录音入口尚未扩展，不以静态代码检查代替跨应用实测。
- 收尾：临时事件监听已注销，调试面板关闭，测试音频暂停；自动学习恢复关闭、学习绑定恢复“跟随默认”、登录启动恢复关闭。应用保留运行中。凭据只在已授权本机配置中使用，未进入仓库。

本轮结果是新增通过项、两个已复现的问题类别和明确的未覆盖项；不是完整 Mac 验收通过。修复应先覆盖两条输入路径的完整剪贴板快照/恢复，再处理学习模型预算及错误反馈，并补齐学习与其他应用的验收入口。


### 剪贴板与学习修复复验（2026-09-26，`ba81da5`）

代码提交 `ba81da5d18cf7541c189627d7218c7ed69c8ac7c` 已推送到 `codex/macos-platform`。本机测试包 CDHash 为 `3b8873d5132f1569641669f84348d53d7abfaff2`。本节区分实机、独立原生测试和接口测试；没有将未取得证据的步骤计为通过。

**实现与回归证据**

- 听写与助手共用 `ClipboardGuard`，由平台选择 `ClipboardSession`。Mac 保存全部 pasteboard item/type 对应的数据（含空剪贴板），恢复时重建原项目；快照不可完整读取、超过 64 MiB 或读取中变化则先拒绝操作。显式“复制结果”继续保留结果，不参与临时恢复。
- 会话记录原生 revision，在写入/粘贴前检查；新复制内容优先，恢复是一次性的。应用内临时事务互斥，防止两个自己的事务互相覆盖。NSPasteboard 不提供原子 compare-and-swap，最终检查/写入仍有不可消除的微小时间窗口；外部复制的来源也不能仅凭文本和 revision 绝对区分。不能声称已实现所有外部并发的原子隔离。
- Windows 使用独立适配器保留原有纯文本恢复能力，增加 sequence number 与一次恢复保护。Windows 富文本保护未在本次实现，桌面行为仍待 Windows 实测。
- 学习判断从 256 调到 1024 token，保留 5 秒超时；空正文明确返回失败，不把 reasoning_content 当 JSON。真实 HTTP 回归先复现“正文缺少 JSON”失败，修复后通过；普通短 JSON 模型与空正文拒绝也通过。
- 真实 `LlmJudge` 使用已授权 DeepSeek 配置，2.14 秒返回学习建议。额外长上下文请求分别 4.11 / 4.50 秒、770 / 790 completion token，均正常返回 Kubernetes JSON。网络或模型超过现有 5 秒边界仍会失败跳过，本轮没有改变超时策略。
- 独立原生测试使用唯一命名的测试剪贴板，不改用户系统剪贴板：45 个断言覆盖富文本、PNG、多项目/file URL、空元数据、空剪贴板、新复制优先、内容相同但 revision 更新、一次恢复、快照拒绝、助手选区认领。临时副本重新引入旧的纯文本策略后，回归断言按预期失败。原有 32 项键盘/目标原生验收继续通过。
- 本机完整 Rust library 210 项通过、4 项交互桌面测试忽略；之后新增 1 项真实 TextEdit 文本的差异筛选回归并通过。前端 86 项通过，默认 Cargo check、前端 build、ATDD app 打包均通过。新的真实 API 用例默认 ignored，不向 CI 传递本地配置或密钥。
- [本提交 Windows/macOS 完整 CI](https://github.com/yyyzl/push-2-talk/actions/runs/36227679375) 两平台均通过，包括 Rust/TypeScript、前端构建和默认功能原型打包；最新 library 计数为 Mac 211、Windows 207（各 4 ignored）。Mac CI 也执行私有剪贴板断言。Windows CI 不替代桌面交互实测。

**实机复验**

- A28 富文本：先从 TextEdit 专用源复制 `PTT_RICH_20260926`（Helvetica 18、加粗）。Chrome `1790407918784233000` 真实录音、选区读取、DeepSeek 翻译与正式回填成功，英文被“会议明天三点开始。”替换，统计 48→49。随后在全新 `/tmp/ptt-atdd-rich-repaired.rtf` 中粘贴；粘贴前明确为 Helvetica 12 常规体，粘贴后标记仍为 18 号加粗。避免用曾粘过富文本的撤销文档造成格式假阳性。
- A28 图片：从预览中的专用测试图复制，成功“从剪贴板新建”证明初始为图像。TextEdit `1790408120976265000` 真实听写成功，统计 49→50；修改测试术语后再在预览 Cmd+N，新建“未命名3”显示同一应用图标，已取得实际截图。原先图片被识别文字覆盖的场景已消除。
- A30：观察器只记录实际 `vocabulary_learning_suggestion` 事件。`1790408406787572000` 修正“库伯内克斯”→Kubernetes 后收到专业术语建议；`1790408651072480000` 后实际“词库学习”窗口显示 Kubernetes、原词/修正及理由。整轮收到 3 次真实建议。
- 后续为点击通知进行了数轮录音；其中有纠词超出 15 秒观察期、读取时 5 秒通知已消失或尚未取得窗口的情况。未逐轮取得内部阶段诊断，不将未收到事件的运行全部解释为某一个原因，也不计这些运行通过。通知聚焦使用测试控制台的窗口 API，只在真实建议事件后执行，没有伪造建议或改动倒计时。未成功完成通知按钮点击验收。
- 入库功能另行验证：从已收到的真实建议取 word，调用按钮使用的正式 `add_learned_word` 命令（source=auto）；实际词库页面显示 `Kubernetes 自动`，之后用词库页面删除此唯一测试词条，列表回到 0。这是后端保存与页面同步证据，不冒充通知按钮点击证据。
- 收尾：两个临时监听器及回调已注销，Web Inspector 关闭，测试音频已暂停；学习开关恢复关闭，学习绑定恢复“跟随默认”，测试词条已清理。应用三项权限有效、运行中，统计 56 / 4,268。原 Windows 配置和 API 密钥没有修改或提交。

本轮已修复并实证复验剪贴板格式丢失与学习 token 预算不兼容；通知点击、学习去重/失焦、其他应用等矩阵仍按未完成项保留。

### 通知、学习并发与 Safari 补测（2026-09-26）

**通知控件实机通过（固定事件重放，非完整录音端到端）**

- 沿用 `ba81da5` 包，在本地调试面板通过正常 Tauri 事件接口发送明确标记为 ATDD 的 Kubernetes 建议，不伪造 ASR 或 LLM 调用。自动工具取得原生“词库学习”窗口，倒计时仍为 5s 时实际点击“添加到词典”；正式词库页面显示 `Kubernetes 自动`、全部 1 / 自动 1。随后在词库页面删除唯一测试词条，恢复 0。
- 同时重放两条不同 ID、同一词汇的建议，只出现一组添加/忽略按钮。实际点击“忽略此建议”后通知退出，正式词库页面仍为全部 0 / 自动 0。没有修改生产倒计时或替换按钮处理函数。此次没有注册持久事件监听，调试面板已关闭。
- 这补齐通知点击、入库、忽略和展示去重；前节真实录音生成建议的证据继续单独记录，不能拼接成一次完整端到端点击验收。

**学习观察回归与修复**

- 确认旧任务结束时无条件从注册表移除同目标记录：按“第一次观察 → 第二次观察 → 第一次结束 → 第三次观察”的顺序，第三次原本无法取消第二次。提取原清理行为后回归先失败（其余 4 项通过），改为比较取消标志身份后通过。
- 注册与替换在同一临界区、`spawn` 之前完成；RAII 守卫随 future 持有，即使 future 尚未执行就被丢弃也清理。不同目标互不取消，快速完成没有残留注册。
- 真实观察循环接受可注入的文本读取边界，以测试相同生产轮询逻辑。旧行为在“首轮前已取消”和“500ms 等待中被替换”两个用例均错误返回新插入文字；修复后取消前没有样本就返回空，有此前样本则保留此前样本，不做取消后的兜底读取。另覆盖原生读取中取消，丢弃进行中的新样本。
- 失焦用例验证连续 3 次失焦停止，完全未读到时不读取旁路控件；已有有效样本时只返回失焦前样本；不支持文本读取的控件返回空。这里是可控边界的自动测试，不声称已覆盖每个真实应用的学习观察。
- Mac 原生文本读取新增 6 项验收：原目标读取、其他应用失焦、同窗替换控件、安全控件、不支持/非文本值、AXValue 读取中切走焦点。最后一项先断言失败；修复为读取后再次检查原目标焦点及安全状态后通过。测试仅使用内存 AX 节点，不读取用户应用数据。

**Safari 驱动与实机目标检查**

- 本地 ATDD 新增 Safari 选项及 `.safari.html` 专用文件身份；只接受 Safari、当前测试文件 URL、AXTextArea 和固定内容/选区，不接受普通 Chrome 测试文件或远端 URL。
- 初次实测安全停止：Safari 首次启动曾把焦点放在地址栏；下一轮输入框虽已聚焦，旧的窗口 AXDocument 检查仍不匹配。驱动补充从该输入框的有界祖先链读取 AXWebArea URL，拒绝其他文档、错误类型和循环。对应原生回归先失败后通过。
- 最终包 CDHash `9f0a56e4afe7df1318e502acd295689269a7caf0`：普通输入目标检查通过；英文选区文档 `1790410923912143000` 检查通过，原生报告应用/窗口/输入框焦点 `1/1/1`、选区准备匹配，Safari AX 实际选中文本精确为 `The meeting starts at three tomorrow.`。
- 上述两轮不使用麦克风、不调用 ASR/LLM，不计 Safari 真实听写或翻译回填通过。最终包已恢复辅助功能与输入监控；16:22:17 系统日志确认当前进程的麦克风 `AUTHREQ_PROMPTING`，等待用户处理受保护的系统确认。服务保持停止、统计仍 56 / 4,268；学习关闭、词库无临时词条。

**本机质量检查**

- 全量 Rust 测试 221 项 library 通过 / 4 ignored，各 integration suite 通过；之后新增的“原生读取中替换”定向测试也通过。前端 86 项通过，前端构建、默认 Cargo check 和最终 ATDD app 打包通过。
- 最终独立原生脚本通过 41 项键盘/目标/读取/驱动验收及 45 个私有剪贴板断言。提交前检查影响范围限于共享学习观察、Mac 文本读取及本地 ATDD；Windows 原生输入与 UIA 实现未改，仍须双平台 CI 验证共享代码。
- 待续：本包的真实学习/录音复验、Safari 真实听写和助手回填、其他应用与悬浮窗视觉矩阵。只有受保护的系统授权需要用户操作，测试执行仍由自动工具负责。
