[package]
name = "push-to-talk"
version = "1.5.4"
description = "A push-to-talk speech-to-text application"
authors = ["you"]
license = ""
repository = ""
edition = "2021"
default-run = "push-to-talk"

[lib]
name = "push_to_talk_lib"
crate-type = ["lib", "cdylib", "staticlib"]

[[bin]]
name = "push-to-talk"
path = "src/main.rs"

[[bin]]
name = "test_api"
path = "src/test_api.rs"


# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[build-dependencies]
tauri-build = { version = "2.0", features = [] }

[dependencies]
tauri = { version = "2.0", features = ["tray-icon"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["full"] }
reqwest = { version = "0.11", features = ["json", "multipart", "stream"] }
cpal = "0.15"
hound = "3.5"
arboard = "3.4"
anyhow = "1.0"
tracing = "0.1"
tracing-subscriber = "0.3"
dirs = "5.0"
base64 = "0.22"
rodio = "0.17"
uuid = { version = "1.0", features = ["v4"] }
flate2 = "1.0"
lazy_static = "1.4"
md5 = "0.7"
sha2 = "0.10"

# WebSocket 实时 ASR 支持
tokio-tungstenite = { version = "0.21", features = ["native-tls"] }
futures-util = "0.3"
crossbeam-channel = "0.5"

# 开机自启动
tauri-plugin-autostart = "2"

# 自动更新
tauri-plugin-updater = "2"
tauri-plugin-process = "2"

# 打开外部链接
tauri-plugin-opener = "2"

# 单实例支持
tauri-plugin-single-instance = "2"

# Windows Audio Session API - 录音时静音其他应用
# Win32 Input API - 键盘模拟（替代 enigo）
[target.'cfg(windows)'.dependencies]
windows = { version = "0.58", features = [
    "Win32_Media_Audio",
    "Win32_System_Com",
    "Win32_Foundation",
    "Win32_UI_Accessibility",         # UI Automation API - 无干扰文本读取
    "Win32_UI_Input_KeyboardAndMouse",
    "Win32_UI_WindowsAndMessaging",  # 焦点管理: GetForegroundWindow, SetForegroundWindow, IsWindow
    "Win32_System_Threading",         # 焦点管理: GetCurrentThreadId, AttachThreadInput, GetWindowThreadProcessId
] }

[features]
# This feature is used for production builds or when a dev server is not specified, DO NOT REMOVE!!
custom-protocol = ["tauri/custom-protocol"]
