import React from "react";
import ReactDOM from "react-dom/client";
import CorrectionWindow from "./CorrectionWindow";
import "../index.css";

ReactDOM.createRoot(document.getElementById("correction-root")!).render(
  <React.StrictMode>
    <CorrectionWindow />
  </React.StrictMode>
);
